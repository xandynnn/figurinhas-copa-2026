import { Box, styled } from "@mui/material";

export const FullHeightBox = styled(Box)({
  height: "100vh",
  width: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});
